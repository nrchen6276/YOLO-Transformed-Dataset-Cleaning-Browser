# YOLO Transformed Dataset Cleaning Browser V2.2.2_202606042205

## English

V2.2.2 is a focused interaction hotfix after V2.2.1. It keeps the standalone executable bundled-core fix, the V2.2 automatic move workflow, and the high-throughput source-group review path.

### Highlights

- Undo keeps the current review directory open and refreshes the current directory in place.
- The UI attempts to return to the restored prefix after undo.
- Selected image cards again show strong HKU Academic Red `#EF4022` feedback.
- Mouse and numeric-key selection share the same red-highlight path.
- The bottom Move Queue / Recent Events area is resizable.

### Validation

- V2.2.2 Qt tests: `8/8 OK`
- V2.2.1 Qt regression: `8/8 OK`
- V2.1 Qt regression: `5/5 OK`
- V1.9.1 Qt regression: `7/7 OK`
- V1.8.1 backend regression: `32 OK, 1 skipped`
- Executable smoke and copied-exe smoke: passed
- Release zip smoke: passed
- Raw dataset raster count in `Dataset/Select_Programme`: `0`

### Asset

Download:

`YOLO_Transformed_Dataset_Cleaning_Browser_V2.2.2_202606042205.zip`

The archive contains the Windows executable, source entrypoint, versioned Python package, tests, build metadata, and abstract UI assets. It excludes raw dataset images, labels, model weights, and dataset YAML files.

All operational conclusions remain `PENDING_AUDIT`.

## 中文

V2.2.2 是 V2.2.1 之后的交互热修复版本。它保留单文件 exe 内置 core 修复、V2.2 自动移动工作流和高速图源组筛选链路。

### 更新重点

- 撤销上一组后保持当前筛选目录打开，并在原地刷新当前目录状态。
- 撤销后会尽量跳回刚恢复的 prefix。
- 图片卡片恢复明显的 HKU 红色 `#EF4022` 选中反馈。
- 鼠标点击和数字键选择使用同一套红色高亮路径。
- 底部任务队列与 Recent Events 区域可拖动调整。

### 验证

- V2.2.2 Qt 测试：`8/8 OK`
- V2.2.1 Qt 回归：`8/8 OK`
- V2.1 Qt 回归：`5/5 OK`
- V1.9.1 Qt 回归：`7/7 OK`
- V1.8.1 后端回归：`32 OK, 1 skipped`
- exe smoke 与复制到临时目录后的 standalone exe smoke：通过
- release zip smoke：通过
- `Dataset/Select_Programme` 中 raw dataset raster 文件数：`0`

### 下载资产

`YOLO_Transformed_Dataset_Cleaning_Browser_V2.2.2_202606042205.zip`

压缩包包含 Windows exe、源码入口、版本化 Python 包、测试、构建元数据和抽象 UI 资产，不包含原始数据集图片、标签、模型权重或数据集 YAML。

所有运行和治理结论仍保持 `PENDING_AUDIT`。
