# YOLO 变换数据集清洗浏览器

这是一个面向 YOLO 视觉训练数据集的桌面清洗工具，用于在多来源、变换增强、合并后的图片组中进行人工复核。它的核心目标是让人工快速比较同组相关图片，选择应保留的图源，并把其他变换图同步归入输出分支，同时保持标签同步、操作可审计、失败可追踪。

当前最新版本：**V2.2.4_202606042313**

## 工具能力

- 打开数据清洗流程生成的图片组复核目录。
- 在同一界面中展示同组图片，支持小键盘式布局。
- 人工选择图源后，同组其他图片作为变换图进入输出分支。
- 图片和 YOLO 标签同步移动，避免只移动图片造成标签错位。
- 支持后台移动队列、撤销上一组、校核摘要、运行日志、紧凑状态提示和非常规分组目录。
- 提供 Windows exe，用户不需要通过命令行启动 Python。

## V2.2.4 更新重点

- Qt 前端切换到 V1.8.2 core 线。
- 支持 `images/special` 这类非常规筛选目录，只要存在对应的 `labels/special`。
- 支持同一个筛选目录内混合出现 2 图组、3 图组、单张组等动态分组，而不是只依赖固定的 `ManualReview_GroupSize_N`。
- 保留 V2.2.3 的紧凑状态 chip、中文单行 Safe Gate badge、撤销原地刷新、红色选中反馈和底部可拖动面板。

## 下载

请下载 release asset：

`YOLO_Transformed_Dataset_Cleaning_Browser_V2.2.4_202606042313.zip`

压缩包包含 exe、源码入口、V1.8.2 core、版本化包、测试、构建元数据和抽象 UI 资产。压缩包不包含原始数据集图片、标签、模型权重或数据集 YAML。

## 安全边界

本工具服务于需要人工确认的数据清洗流程。所有文件移动都应保持明确、可审计，并尽量可恢复。发布包只包含程序产物，不包含原始训练数据。

所有数据治理结论仍保持 **PENDING_AUDIT**，不能直接升级为论文、模型或数据质量实证结论。
